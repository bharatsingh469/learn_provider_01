import 'package:flutter/material.dart';

class NotifyLisstnerScreen extends StatelessWidget {
  const NotifyLisstnerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Use STL as STF'),
        backgroundColor: Colors.red,
        centerTitle: true,
      ),
      body: Center(
        child: Text("Ram Ram Ji"),
      ),
    );
  }
}
